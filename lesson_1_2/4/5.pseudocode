forward_step = 20
path_width = 10

for i in range(walls):
    forward(10)
    penup()
    forward(path_width)
    pendown()
    forward(forward_step - 10)
    left(90)
    forward_step = forward_step + path_width